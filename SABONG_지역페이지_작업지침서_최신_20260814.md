# SABONG 지역 입주청소·이사청소 페이지 작업지침서

최종 기준일: 2026-08-14\
기준 사이트: `sabong.co.kr`

## 1. 목적

사봉 지역페이지는 `지역명 + 입주청소 / 이사청소 / 청소비용 / 견적`
검색의도에 대응하는 지역 랜딩페이지다.

페이지 수를 늘리는 것보다 다음을 우선한다.

-   검색엔진이 각 지역페이지를 서로 다른 문서로 이해할 수 있는 실질적
    고유성
-   실제 사용자가 읽었을 때 자연스러운 청소 견적 안내
-   기존 디자인·CTA·공통 서비스 설명의 일관성
-   title, description, canonical, OG, Schema, H1, alt 등 기술 SEO의
    무결성
-   신규 페이지 추가 후 누적 전체 기준 회귀검사

단순 지역명 치환 페이지를 대량 생성하지 않는다.

### 검색 노출 우선 기준

사봉은 검색 노출 검수와 개선 판단에서 **네이버 검색결과를 우선 기준**으로
삼고 구글은 보조적으로 확인한다.

-   네이버 검색결과에 사용될 수 있도록 페이지별 고유한 `title`과
    `meta description`을 작성한다.
-   검색 스니펫은 검색어와 문서 내용에 따라 본문에서 다시 추출될 수
    있으므로 Hero 첫 2~4문장도 자연스럽고 고유하게 유지한다.
-   `og:title`, `og:description`, `og:image`, canonical, Schema는 실제
    페이지 내용 및 URL과 일치시킨다.
-   파비콘이나 대표 이미지 변경은 자동 지역 생성 작업에 포함하지 않는다.
    사용자가 명시적으로 요청한 경우에만 별도 검수 후 변경한다.
-   변경 후 네이버 재수집 요청은 선택 사항이다. sitemap과 내부링크가
    정상이라면 자연 재수집을 기다려도 된다.

------------------------------------------------------------------------

## 2. 현재 구조 확인 원칙

페이지 수와 이미지 수를 이 문서에 고정 숫자로 기록하지 않는다. 작업을
시작할 때마다 다음 파일과 검수 결과에서 동적으로 확인한다.

-   지역페이지 및 단계별 수: `maps/region_map.csv`
-   사람이 보는 진행현황: `maps/progress.md`
-   실제 HTML·메타·링크·Schema·sitemap 상태: `python validate_regions.py`
-   ZIP 경로와 파일 수: 최종 ZIP central directory

2026-08-14 이후 신규 페이지의 Hero는 **90~180자**, 지역 고유본문은
**450~650자**, 두 영역의 합계는 **최소 550자**로 작성한다. 고유본문 숫자는
제목을 제외한 직접 하위 본문 `<p>`만 계산한다.

------------------------------------------------------------------------

## 3. 절대 보존 영역

신규 지역 추가나 문구 고유화 작업에서 필요 없는 구조 변경을 하지 않는다.

기본적으로 유지: - 전체 레이아웃과 CSS class 구조 - Hero 디자인 - CTA
위치와 링크 구조 - 서비스 특징/체크리스트 영역 - 비용·견적 안내 공통
영역 - FAQ 디자인과 동작 - 공통 이미지 및 이미지 경로 - 기존
footer/header - 기존 정상 canonical/Schema 구조

SEO 개선을 이유로 사이트 전체를 리팩터링하거나 디자인을 다시 만들지
않는다.

------------------------------------------------------------------------

## 4. 상단 Hero 검색의도 문구 --- 고유화 필수

과거 지역페이지 대부분이 다음과 같은 한 문장을 반복했다.

`OO 입주청소부터 이사청소까지, 우리 집 청소 비용을 간편하게 비교해보세요.`

검색결과 snippet이 이 공통문장을 가져갈 경우 여러 지역페이지가
검색결과에서 거의 같은 설명으로 보일 수 있으므로, 2026-08-08부터 상단
Hero 본문도 고유화한다.

### 작성 기준

-   페이지당 **2\~4문장**
-   지역 특성 소개가 아니라 **청소 검색의도 중심**
-   첫 1\~2문장 안에 지역명과 핵심 서비스 키워드를 자연스럽게 배치
-   지역명은 문맥상 필요하면 1\~2회 사용
-   모든 페이지에서 정확히 같은 위치·횟수로 지역명을 반복하지 않음
-   억지 동의어로 유사도 0%를 만들지 않음

활용 가능한 핵심 검색의도: - `OO 입주청소` - `OO 이사청소` -
`입주청소 비용` - `이사청소 비용` - `청소업체` - `청소 견적` -
`견적 비교` - `청소 범위` - `추가비용` - `작업 일정` - `평수 / 면적` -
`우리 집 청소비용`

페이지별로 검색의도 조합과 정보 순서를 분산한다.

예: - 입주청소 + 비용 + 견적 - 이사청소 + 업체 + 작업범위 -
입주·이사청소 + 일정 + 견적비교 - 청소업체 + 추가비용 + 포함범위 -
면적 + 현장상태 + 예상비용

### 금지

-   5\~10개의 고정 문장 템플릿에 지역명만 바꿔 대량 순환
-   모든 페이지가 동일한 문장 순서
-   `최고`, `무조건`, `가장 저렴`, `100%` 등 근거 없는 과장
-   지역의 관광지·상권·인구 특성을 억지로 Hero에 넣어 고유성을 만드는
    방식
-   사람이 실제로 쓰지 않는 어색한 SEO 동의어

상단 짧은 문구는 핵심어 특성상 일부 단어가 반복되는 것이 정상이다.
**완전 동일 문구 0건과 자연스러운 문장 구조 분산**을 우선하며, 짧은 상단
문구에 지나치게 낮은 유사도 수치를 강제하지 않는다.

2026-08-08 기준 지역명을 마스킹한 상단 5-word shingle 누적 최고 유사도는
약 **40.625%**이며 exact duplicate는 0건이다. 짧은 검색의도 문구에서는
이 정도의 핵심어 중첩 자체를 오류로 보지 않는다.

------------------------------------------------------------------------

## 5. 지역명 생성: 대표 동명 우선

검색 사용자가 실제로 쓰는 대표 동명을 페이지명으로 사용한다. 행정 편의를
위해 숫자나 일·이·삼으로 분할된 행정동은 각각 별도 페이지로 만들지 않는다.

-   `역삼1동`, `역삼2동` → `역삼동`
-   `대저일동`, `대저이동` → `대저동`
-   `판암1동`, `판암2동` → `판암동`
-   `가양1동`, `가양2동` → `가양동`
-   `영종1동`, `영종2동`, `영종3동` → `영종동`
-   `운서1동`, `운서2동` → `운서동`

이는 단순 문자열 치환 규칙이 아니다. `수성동1가`처럼 숫자가 실제
법정동·주소 명칭에 포함된 지역은 숫자를 제거하거나 다른 동과 합치지 않는다.
신규 생성 전 실제 지역 단위를 구분하고, 대표 페이지로 통합한 경우
canonical·Breadcrumb·JSON-LD·내부링크·region_map·sitemap도 모두 대표
페이지를 가리키는지 확인한다. 삭제된 분할 동 페이지는 다시 생성하지 않는다.

------------------------------------------------------------------------

## 6. 지역 고유본문 `.region-intro-box`

사봉 페이지의 핵심 실질 고유성 영역이다.

### 분량

2026-08-14 이후 신규 페이지 기준: - Hero: **90\~180자** - 지역 고유본문:
**450\~650자** - Hero와 지역 고유본문 합계: **최소 550자**. 지역 고유본문은
제목을 제외한 직접 하위 `<p>` 본문만 계산하며, 650자를 넘긴 채 완료 처리하지
않는다.

기존 공개 페이지는 일괄 증문으로 새로운 반복문을 만드는 일을 막기 위해 종전
320~550자 범위로 검수한다. 다만 기존 페이지라도 핵심본문을 전면 개정하거나
신규 페이지로 다시 생성하면 강화 기준을 적용한다.

유사도를 낮추기 위해 기존 문단 끝에 공통 문장이나 보조 문단을 덧붙이는
방식은 금지한다. 특히 동일 문장을 연속으로 붙이거나, 여러 페이지에 같은
마무리 문장을 일괄 삽입하거나, 분량 상한을 넘겨 수치만 낮추는 작업은
고유화로 인정하지 않는다. 기존 도입부와 핵심 본문 자체를 페이지별로 다시
작성한 뒤 다음 두 조건을 모두 통과해야 한다.

-   같은 문장이 한 페이지 안에서 두 번 이상 반복되지 않음
-   같은 도시 내 지역페이지 최고 5-word shingle 유사도 45% 이하

검수기는 위 두 조건과 신규 450~650자 범위를 오류로 처리해야 하며, 수동 검수에서도
문장 연결이 자연스러운지 확인한다. 문장을 중간에서 자르거나 조사·서술어가
누락된 상태로 분량을 맞추면 안 된다.

### 내용 구성

지역명만 바꾸지 말고 다음 요소 중 실제 문맥에 맞는 내용을 조합한다.

-   주거 형태 차이에 따른 청소 준비
-   신축/기존 거주 공간의 청소 포인트
-   아파트·빌라·오피스텔·주택 등 구조 차이
-   창틀, 주방, 욕실, 베란다, 수납공간 등 작업량 차이
-   공사 분진과 생활 오염의 차이
-   이사 날짜와 청소 일정 조율
-   주차·엘리베이터·작업 동선 확인
-   도배·인테리어 등 다른 공정과 청소 순서
-   사진/현장상태 전달과 견적 정확도
-   추가 작업이나 특이사항 사전 확인

지역에 대해 검증되지 않은 구체적 사실을 만들어내지 않는다. 특정 지역
특성을 사용할 때는 실제로 안전하게 말할 수 있는 범위에서 작성한다.

### 구조

현재 페이지처럼 여러 짧은 문단으로 나눠 가독성을 유지한다. 하나의 긴
문단 하나에 전체 분량을 몰아넣지 않는다.

------------------------------------------------------------------------

## 7. 상단과 지역 고유본문의 역할 분리

사봉은 다음의 2단 고유화 구조를 유지한다.

**상단 Hero** - 입주청소/이사청소 검색의도 - 비용·견적·업체 비교 의도 -
검색결과 snippet 후보 - 2\~4문장 고유화

**지역 고유본문** - 실제 청소 준비에 도움이 되는 구체적 내용 - 약
450\~650자 - 지역별 실질적 콘텐츠 차별화

상단에서 지역 특성을 억지로 늘리고 하단에서 같은 내용을 반복하지 않는다.

------------------------------------------------------------------------

## 8. Title 규칙

기본적으로 지역명과 입주청소/이사청소 핵심어를 포함한다.

지역명만 바꾸고 나머지 제목을 전 페이지에 동일하게 적용하지 않는다.
`가격`, `비용`, `견적`, `업체`, `추천`, `비교`, `상담` 등 실제 검색의도
단어를 페이지 문맥에 맞게 자연스럽게 조합한다. 짧은 TITLE 특성상 일부
조합이 반복될 수는 있으나, 지역명을 가린 동일 조합이 시 단위 3개·구 단위
3개·동 단위 3개를 초과해 대량 반복되면 배포하지 않는다.

동일한 `중구`, `동구`, `서구`처럼 여러 도시에 같은 행정구역명이 존재하면
사이트 전체에서 title이 완전히 같아질 수 있다.

이 경우 반드시 상위 도시명을 붙여 구분한다.

예: - `대구 중구 ...` - `인천 중구 ...` - `대전 중구 ...`

title 수정 시 함께 확인: - `og:title` - `twitter:title` - WebPage/WebSite
Schema `name`. 네 항목은 반드시 같은 값으로 동기화한다.

사이트 전체 duplicate title 0건을 배포 기준으로 한다.

------------------------------------------------------------------------

## 9. Meta description 규칙

-   해당 지역명 포함
-   입주청소/이사청소/견적/비용 중 핵심 검색의도 포함
-   페이지 내용을 실제로 설명
-   같은 행정구역명 때문에 완전 동일 description이 생기면 도시명을
    추가해 구분
-   과도한 키워드 나열 금지
-   빈 description 금지
-   지역명만 바꾼 동일 description의 대량 반복 금지
-   각 페이지의 고유화된 Hero 또는 지역 고유본문 내용을 실제로 반영
-   `og:description`, `twitter:description`, WebPage/WebSite Schema
    `description`과 반드시 동기화

지역페이지 전체 exact duplicate description 0건을 목표로 한다. 지역명을
가린 동일 description도 단계별 최대 2개를 넘지 않도록 검수한다.

------------------------------------------------------------------------

## 10. H1

-   페이지당 H1 정확히 1개
-   해당 지역명과 핵심 서비스가 자연스럽게 포함
-   디자인용 문구를 추가하면서 H1을 중복 생성하지 않음

배포 기준: 모든 HTML H1 = 1개.

------------------------------------------------------------------------

## 11. Canonical / OG

각 페이지 canonical은 자기 자신의 최종 공개 URL이어야 한다.

검사: - canonical 누락 0건 - canonical 중복/오지정 0건 - `og:url` 존재
및 현재 페이지 URL과 일치 - title 변경 시 OG title 동기화 - description
변경 시 OG description 동기화

일반 안내 페이지도 필요한 메타가 빠지지 않았는지 확인한다.

------------------------------------------------------------------------

## 12. Schema / JSON-LD

JSON-LD는 수정 후 반드시 파싱검사한다.

지역페이지에서 구조화데이터가 있는 경우: - 현재 페이지 URL - 지역명 -
WebPage 이름/설명 - Breadcrumb - Service 또는 제공 서비스 정보 등이 실제
HTML과 충돌하지 않는지 확인한다.

복사본의 이전 지역명·이전 URL이 Schema에 남는 것을 금지한다.

------------------------------------------------------------------------

## 13. 이미지 ALT

모든 의미 있는 이미지에 alt를 유지한다.

검사: - `alt` 속성 자체가 없는 이미지 0건 - 의미 있는 이미지의 빈
`alt=""` 0건 - 다른 지역명을 복사한 alt 잔존 금지 - 이미지 내용과 무관한
키워드 나열 금지

이미지 총수는 고정 기록하지 않는다. 매 작업의 최신 저장소 전체에서 alt
누락·빈 alt·잘못된 지역명 잔존을 동적으로 검사한다.

------------------------------------------------------------------------

## 14. 지역명 치환 잔존 검사

신규 페이지를 기존 페이지에서 복사해 만들었다면 HTML 전체에서 반드시
확인한다.

-   이전 지역명
-   이전 도시명
-   이전 canonical URL
-   이전 OG URL
-   이전 Schema URL/지역
-   이전 Breadcrumb
-   이전 alt
-   이전 title/description

눈에 보이는 본문만 바꾸고 head/Schema에 이전 지역을 남기면 배포하지
않는다.

------------------------------------------------------------------------

## 15. 내부링크 / Sitemap / Robots

신규 URL 추가 시: - sitemap에 실제 공개 URL 포함 여부 확인 - 존재하지
않는 미래 지역페이지를 미리 내부링크하지 않음 - 삭제/변경된 URL을 잘못
연결하지 않음 - robots/noindex 상태 확인 - index 대상 페이지가 실수로
noindex가 되지 않도록 확인

------------------------------------------------------------------------

## 16. 콘텐츠 중복 QA

중복검사는 한 영역만 보지 않는다.

### 상단 Hero

-   exact duplicate 0건
-   지역명 마스킹 후 문장 구조가 지나치게 동일한 페이지 쌍 확인
-   핵심어 반복 자체는 오류로 보지 않음
-   숫자를 낮추기 위한 부자연스러운 문장 금지

### `.region-intro-box`

-   지역명 마스킹 후 누적 전체와 비교
-   동일 문단/긴 구절 재사용 여부 확인
-   신규 페이지끼리뿐 아니라 기존 전체 페이지와 교차검사

특정 유사도 숫자 하나만으로 품질을 판단하지 않고 실제 반복 블록의 길이와
내용도 함께 본다.

------------------------------------------------------------------------

## 17. 신규 지역페이지 작업 순서

1.  최신 전체 소스를 기준본으로 사용
2.  추가할 도시/구/지역과 최종 URL 확정
3.  기존 같은 이름의 행정구역이 사이트에 있는지 검색
4.  기존 정상 페이지 구조를 복사하되 불필요한 리팩터링 금지
5.  title/H1/description/canonical/OG/Schema를 새 지역으로 정확히 변경
6.  Hero 90\~180자를 검색의도 중심의 2\~4문장으로 새로 작성
7.  `.region-intro-box` 450\~650자를 새로 작성하고 두 영역 합계 550자 이상 확인
8.  alt와 이전 지역 잔존 확인
9.  `.github/scripts/generate_internal_links.py`로 내부링크 10개 재계산
10. `.github/scripts/generate_sitemap.py`로 map 기준 sitemap 생성
11. 전체 HTML 파싱검사
12. H1/canonical/OG/Schema/alt 전수검사
13. title/description exact duplicate 검사
14. Hero exact duplicate 및 누적 유사도 검사
15. 지역 고유본문 누적 중복검사
16. 모든 검사 통과 후 변경 파일만 원래 경로 그대로 ZIP 배포

내부링크는 임의 목록을 복사하지 않는다. 생성기는 시 페이지에서 구·군을,
구·군 페이지에서 하위 동·읍·면을, 동·읍·면 페이지에서 같은 구의 다른
지역을 먼저 배치한다. 10개가 부족할 때만 부모 페이지와 같은 도시의 다른
구·지역으로 보충한다. 동일한 10개 링크 묶음은 반복하지 않는다.

------------------------------------------------------------------------

## 18. 배포 금지 조건

다음 중 하나라도 있으면 사용자에게 업로드 ZIP을 넘기지 않는다.

-   H1 0개 또는 2개 이상
-   canonical 누락/오지정
-   JSON-LD 파싱 오류
-   이전 지역명/URL 잔존
-   이미지 alt 누락
-   지역페이지 duplicate title
-   의도하지 않은 duplicate description
-   Hero 문구 완전중복
-   `.region-intro-box`가 단순 지역명 치환
-   존재하지 않는 내부링크
-   sitemap 누락
-   기존 정상 디자인/CTA/기능 회귀

------------------------------------------------------------------------

## 19. 수정 범위 최소화 원칙

사봉은 이미 디자인과 공통 서비스 구조가 잡혀 있으므로, 지역 확장
작업에서 전체 사이트를 재설계하지 않는다.

항상: 1. 작업 시작 전 최신본 상태 확인 2. 변경해야 할 범위 확정 3.
필요한 파일만 수정 4. 수정 전후 회귀검사 5. 사용자에게는 업로드할 파일만
원래 경로로 ZIP 제공

특정 오류 하나를 고친 뒤 같은 원인이 다른 페이지에도 있는지 누적 전체를
검사한다.

------------------------------------------------------------------------

## 20. 2026-08-08 상단 고유화 작업 결과를 기준본으로 사용

이번 업데이트에서 지역계열 70페이지의 Hero 공통 한 문장을 각각 2\~3문장
검색의도형 문구로 고유화했다.

동시에: - 동일 행정구역명으로 발생하던 title 충돌 제거 - duplicate meta
description 제거 - 일반 페이지의 누락 meta description / OG URL 보완 -
전체 73 HTML H1/canonical/JSON-LD 재검사 - 전체 853 이미지 alt 재검사

최종 결과: - Hero exact duplicate 0건 - 지역명 마스킹 Hero 최고 5-word
shingle 유사도 약 40.625% - title exact duplicate 0건 - description
exact duplicate 0건 - H1/canonical/JSON-LD/alt 오류 0건

앞으로 신규 페이지는 이 상태보다 품질이 후퇴하지 않도록 누적 전체
기준으로 검수한다.

------------------------------------------------------------------------

## 21. 지역 중복 생성 방지 관리체계 --- 2026-08-09 최우선 추가

사봉 지역 수가 늘어날수록 사람이 기존 지역을 기억해서 중복 생성을 막는
방식은 사용하지 않는다.

### 21-1. `maps/region_map.csv`를 지역 발행 기준표로 사용

실제 운영 지역계열 페이지는 모두 `maps/region_map.csv`에 등록한다. 현재
개수를 문서에 하드코딩하지 않고 map 행과 실제 HTML을 동적으로 대조한다.

구성: - `city_key` - `city_name` - `region_name` - `page_type` -
`path` - `canonical_url` - `source_file`

신규 지역 생성 전 반드시 **map + 실제 HTML을 동시에 검사**한다.

배포 금지: - 같은 `city_key + region_name`이 이미 존재 - 같은 canonical
URL이 이미 존재 - 같은 source path가 이미 존재 - map에는 있는데 HTML이
없음 - HTML은 있는데 map에 없음

`중구`, `동구`, `서구`처럼 여러 도시에 존재하는 이름은 지역명 하나만으로
중복 판정하지 않는다. 반드시 상위 도시와 묶어서 관리한다.

### 21-2. 신규 지역 선정 순서

사용자가 특정 도시의 신규 지역 생성을 요청하면:

1.  최신 ZIP을 기준으로 실제 지역 목록 추출
2.  `maps/region_map.csv`와 실제 HTML 양방향 대조
3.  해당 도시에서 이미 발행된 지역 제외
4.  아직 발행되지 않은 지역만 후보로 선정
5.  생성 직전 다시 중복 여부 확인
6.  신규 페이지 생성
7.  map에 신규 행 추가
8.  전체 검수기 실행
9.  PASS일 때만 업로드 ZIP 전달

기존에 올린 지역을 실수로 다시 만드는 것을 가장 먼저 차단한다.

### 21-3. `validate_regions.py` 필수 실행

프로젝트 루트의 `validate_regions.py`는 현재 파일시스템과
`maps/region_map.csv`를 동적으로 읽는다. 도시별 페이지 수를 하드코딩하지
않는다.

현재 검사 항목: - map ↔ 실제 지역 HTML 양방향 일치 - H1 1개 - title /
description 지역명 - canonical - OG URL - JSON-LD 파싱 - 이미지 alt
누락/빈 값 - 신규 Hero 90~180자 - 신규 `.region-intro-box` 450~650자 -
두 영역 합계 550자 이상 - duplicate
title - duplicate description - duplicate canonical - Hero exact
duplicate - 지역명을 마스킹한 Hero exact duplicate - 도시별 sitemap에
canonical 포함 여부 - 상단 Hero 누적 5-word shingle 최고 유사도 보고

`QA FAIL` 상태에서는 배포하지 않는다.

GitHub Actions는 HTML·map·검수기·생성 스크립트 변경 시 자동 실행한다.
map 기반 sitemap을 임시 생성한 뒤 `validate_regions.py`가 통과한 경우에만
sitemap 변경을 커밋한다. 검수 실패 상태의 sitemap이나 HTML은 자동으로
커밋하지 않는다.

### 21-4. 1회 신규 생성량 제한

사봉은 GPT 작업 품질 유지를 위해 **한 번에 신규 지역페이지 최대 10개**를
절대 상한으로 한다.

이 제한은 SEO상의 페이지 발행 제한이 아니라: - 페이지별 Hero 90\~180자,
2\~4문장 개별 작성 - `.region-intro-box` 450\~650자 개별 작성 -
메타/Schema/alt/URL 치환 잔존 확인 - 누적 전체 중복 교차검사 의 품질을
유지하기 위한 작업 단위다.

10개를 초과하는 요청은 10개 이하 묶음으로 나누고, 각 묶음마다 누적 전체
QA를 통과한 뒤 다음 묶음을 진행한다.

### 21-5. 상단 Hero 대규모 확장 원칙

페이지가 수백 개로 늘어나도 상단 문구의 유사도 0%를 억지로 목표로 하지
않는다.

-   지역명은 자연스럽게 1\~2회 사용 가능
-   `입주청소`, `이사청소`, `청소비용`, `견적`, `청소업체`, `작업범위`,
    `추가비용` 등 핵심 검색어 반복은 정상
-   핵심은 문장 전체 exact duplicate와 소수 템플릿 순환을 피하는 것
-   검색의도 조합과 정보 순서, 문장 연결 방식을 분산
-   억지 동의어와 부자연스러운 SEO 문장 금지
-   상단은 검색의도, 하단 `.region-intro-box`는 실질 고유정보라는 2단
    구조 유지

짧은 Hero에는 `.region-intro-box`와 같은 고정 유사도 수치를 강제하지
않는다. exact duplicate 0건과 자연스러운 검색의도 분산을 우선한다.

### 21-6. 기준본 상태 기록 원칙

기준본의 페이지 수·도시 수·유사도 수치는 `maps/progress.md`와 해당 작업의
최종 검수보고서에 기록한다. 이 지침에는 시간이 지나면 틀리게 되는 총개수를
적지 않는다. 배포 기준은 항상 현재 저장소 전체에서 `validate_regions.py`
`QA PASS`, map↔HTML↔sitemap 일치, duplicate title/description 0건이다.

------------------------------------------------------------------------

## 22. 동·읍·면 페이지 확장 구조 --- 2026-08-09 확정

현재 5개 도시 서브도메인 구조를 유지하고, 구·군별 서브도메인을 추가로
분리하지 않는다.

기본 URL/파일 계층:

-   도시 메인: `busan.sabong.co.kr/`
-   구·군 허브: `busan.sabong.co.kr/동래구/`
-   동·읍·면 상세: `busan.sabong.co.kr/동래구/온천동/`

실제 파일 구조:

-   `busan/index.html`
-   `busan/동래구/index.html`
-   `busan/동래구/온천동/index.html`
-   `busan/동래구/사직동/index.html`

즉 **동·읍·면 폴더는 반드시 해당 부모 구·군 폴더 내부에 생성한다.**

구·군 페이지는 하위 동·읍·면 페이지의 지역 허브 역할을 한다. 구·군
페이지에서 실제 생성된 하위 지역으로 연결하고, 동·읍·면 페이지에서는
부모 구·군 및 같은 구·군의 관련 하위 지역으로 자연스럽게 연결한다.

구·군별 별도 서브도메인 분리는 현재 보류한다. 향후 실제 색인·노출
데이터에서 분리 필요성이 확인될 때 별도 검토한다.

### 22-1. 동 단위 SEO 명명

동명이 다른 구·도시에 존재할 수 있으므로 내부 관리키와 SEO 메타에서는
상위 행정구역을 함께 식별한다.

권장 식별 단위:

`city_key + district_name + locality_name`

예: `busan + 동래구 + 온천동`

Title은 필요에 따라 `부산 동래구 온천동 입주청소·이사청소 ...`처럼 상위
지역을 포함하여 사이트 전체 title 충돌을 막는다.

H1은 기존 디자인을 유지하면서 `온천동 입주청소·이사청소`처럼 자연스럽게
작성할 수 있다.


### 22-2. 동·읍·면 페이지 Breadcrumb 표준

동·읍·면 상세 페이지의 화면상 Breadcrumb는 반드시 3단계 계층으로 표시한다.

예:
`서울 이사청소(링크) › 강남구 입주청소(링크) › 역삼동(현재 페이지 텍스트)`

규칙:
- 1단계 도시: `도시명 + 이사청소`, 도시 메인으로 링크
- 2단계 구·군: `구·군명 + 입주청소`, 해당 구·군 허브로 링크
- 3단계 동·읍·면: **지역명만 표시**, 현재 페이지이므로 자기 자신 링크 금지
- 3단계에 `입주청소`, `이사청소`를 반복하지 않는다.
- 화면상 Breadcrumb와 JSON-LD `BreadcrumbList`의 표시명도 동일하게 맞춘다.
- 부모 2개 링크는 지역 계층과 내부링크 구조를 명확하게 하기 위해 유지한다.

------------------------------------------------------------------------

## 23. 한글 폴더명·ZIP 경로 무결성 --- 배포 필수 규칙

2026-08-09 작업 중 일부 실행/추출 환경에서 정상 한글 ZIP 경로가 `#Uac15#Ub0a8#Uad6c` 같은 내부 escape 표현으로 보이는 현상이 확인됐다.

중요:
- 이 표현이 **작업환경에서 보였다는 이유만으로 원본 ZIP이 깨졌다고 판정하지 않는다.**
- 최종 판정 기준은 반드시 **ZIP central directory(실제 ZIP 엔트리명)** 와 원본 저장소의 실제 경로다.
- 실제 ZIP 엔트리가 `seoul/강남구/index.html`처럼 정상 한글이면 정상이다.
- 특정 추출/표시 환경에서만 `#Uxxxx`로 보이는 것은 환경 표현 문제로 취급한다.

### 23-1. 실제 경로 오류와 표시 오류를 구분

**실제 오류**
- 원본 ZIP central directory 자체에 `#Uxxxx`, `%uXXXX`, 문자 그대로의 `\uXXXX`가 파일명으로 저장됨
- 생성한 ZIP을 다시 열었을 때 엔트리명이 escape 문자열로 존재함
- 동일 위치에 정상 한글 폴더와 escape 폴더가 실제로 동시에 존재함
- canonical / sitemap / HTML href / map에 escape 문자열이 실제 URL로 기록됨

**표시/환경 오류**
- 원본 ZIP central directory는 정상 한글인데 특정 작업환경에서 압축 해제 또는 화면 출력 시 `#Uxxxx`처럼 보임
- 이 경우 원본 ZIP을 다시 쓰거나 폴더명을 escape 문자열 기준으로 재생성하지 않는다.

### 23-2. 원본 ZIP 검사 순서

한글 경로 프로젝트를 받으면 압축 해제 결과부터 믿지 말고 다음 순서로 검사한다.

1. 원본 ZIP을 직접 연다.
2. ZIP central directory의 실제 엔트리명을 확인한다.
3. `seoul/강남구`, `busan/동래구` 등 예상 한글 경로가 실제 Unicode 한글로 들어 있는지 확인한다.
4. ZIP 엔트리 자체에 `#U`, `%u`, 문자 그대로의 `\u` 패턴이 있는지 검사한다.
5. 원본 ZIP 엔트리가 정상이라면 이후 작업에서도 **그 Unicode 경로를 기준값으로 고정**한다.
6. 추출 환경에서만 escape 형태로 보이면 해당 추출 결과를 경로 진실값으로 사용하지 않는다.

### 23-3. 수정 작업 시 한글 경로 보존

- 정상 한글 경로를 ASCII/escape 경로로 변환하지 않는다.
- 작업환경에서 `#Uxxxx`로 보인 이름을 그대로 새 폴더명으로 쓰지 않는다.
- 한글 폴더의 대응관계를 임의로 다시 추정하여 복제하지 않는다.
- 원본 ZIP central directory 또는 정상 저장소 경로에서 확인한 실제 Unicode 이름만 사용한다.
- 콘텐츠 수정은 정상 Unicode 경로의 파일에만 반영한다.

### 23-4. ZIP 생성 규칙

업로드 ZIP 생성 시:

1. 원본 실제 Unicode 경로를 그대로 사용한다.
2. ZIP 내부 엔트리명을 별도 escape/ASCII 치환하지 않는다.
3. ZIP 생성 후 반드시 다시 ZIP을 직접 연다.
4. central directory에서 한글 경로가 실제 한글로 유지되는지 확인한다.
5. 실제 ZIP 엔트리에 `#U`, `%u`, 문자 그대로의 `\u` 경로가 1건이라도 있으면 실패 처리한다.
6. 정상 한글 폴더와 escape 폴더가 실제 ZIP 내부에 동시에 존재하면 실패 처리한다.
7. **추출된 임시 작업폴더의 표시만으로 ZIP 오류를 판정하지 않는다.**

### 23-5. 배포 전 경로 전수검사

최종 배포 전 다음을 검사한다.

- 최종 ZIP central directory의 파일/폴더명
- 실제 저장소 디렉터리명
- `href`
- canonical
- `og:url`
- JSON-LD URL/Breadcrumb
- sitemap URL
- `maps/region_map.csv`

판정 우선순위:

**최종 ZIP central directory / 실제 저장소 경로 > 특정 실행환경의 압축해제 표시**

### 23-6. 재발 방지 핵심 원칙

이번 문제처럼 정상 원본을 작업환경의 escape 표시로 오판하면, 정상 한글 폴더를 지우거나 `#Uxxxx` 폴더를 새로 만들어 오히려 실제 경로를 망가뜨릴 수 있다.

따라서 앞으로는:

- `#Uxxxx`가 보이면 먼저 **원본 ZIP 엔트리부터 확인**
- 원본 ZIP이 정상이면 **복구 작업 금지**
- 실제 ZIP 엔트리가 깨진 경우에만 경로 복구
- 복구 후에도 반드시 새 ZIP central directory를 재검사
- 작업 완료 보고에서 “한글 경로 정상/오류”를 말할 때는 **실제 ZIP 엔트리 검사 결과를 근거로만 보고**

이 규칙은 사봉의 모든 향후 지역 확장 작업에 최우선 적용한다.

------------------------------------------------------------------------

## 24. 지역 진행현황 관리 --- 작업 위치를 기억에 의존하지 않음

페이지가 수백\~수천 개로 늘어날 것을 전제로, "어디까지 만들었는지 / 다음
어디를 작업해야 하는지"를 대화 기억으로 관리하지 않는다.

`maps/region_map.csv`를 단순 발행목록에서 **계층형 지역 진행현황
기준표**로 확장한다.

### 24-1. 권장 필드

앞으로 map은 최소 다음 정보를 관리한다.

-   `city_key`
-   `city_name`
-   `district_name`
-   `locality_name`
-   `level`
-   `page_type`
-   `status`
-   `path`
-   `canonical_url`
-   `source_file`
-   `created_date`
-   `batch_id`

`level` 예: - `city` - `district` - `locality`

`status` 예: - `published` - `planned` - `hold`

현재 존재하는 페이지는 `published`로 본다.

### 24-2. 작업현황 요약파일

`maps/progress.md`를 함께 유지한다.

이 파일은 사람이 빠르게 읽기 위한 현황판이며 최소 다음을 표시한다.

-   현재 운영 도시
-   도시별 완료 구·군 수
-   도시별 생성 완료 동·읍·면 수
-   마지막 작업 지역
-   마지막 작업 batch
-   다음 작업 후보/차례
-   전체 published 페이지 수
-   보류 사항

예:

`부산 > 동래구 : 온천동, 사직동 완료 / 다음 후보 명륜동`

정확한 진실의 원본은 `region_map.csv`이고 `progress.md`는 그 내용을 읽기
쉽게 요약한 파일이다. 둘이 충돌하면 map과 실제 HTML을 대조해 복구한 뒤
진행한다.

### 24-3. 신규 작업 시작 전 필수 확인

매 작업 시작 시 순서:

1.  최신 ZIP의 실제 HTML 구조 스캔
2.  깨진 한글 경로 0건 확인
3.  `region_map.csv`와 실제 HTML 양방향 대조
4.  `progress.md`와 map 비교
5.  요청 도시/구의 기존 published 동·읍·면 목록 추출
6.  이미 만든 지역 제외
7.  다음 미생성 지역 선정
8.  최대 10페이지 작업
9.  map 업데이트
10. progress 업데이트
11. 전체 QA
12. ZIP 경로 재검사
13. PASS 후 배포

사용자가 단순히 "다음 10개", "이어서 진행"이라고 해도 이 파일들을 읽으면
직전 작업 위치를 확인할 수 있어야 한다.

### 24-4. 중복 생성 판정키

동·읍·면 페이지는 단순 지역명으로 판정하지 않는다.

기본 고유키:

`city_key | district_name | locality_name | level`

동명이 존재해도 부모 구·군이 다르면 다른 지역으로 처리한다.

같은 고유키가 두 번 존재하면 배포 금지한다.

------------------------------------------------------------------------

## 25. 검수기 확장 요구사항

`validate_regions.py`는 동·읍·면 확장 전에 다음 기능까지 지원하도록
유지/업데이트한다.

-   city → district → locality 계층 검사
-   locality가 실제 부모 district 폴더 아래 존재하는지 검사
-   map의 계층과 실제 파일경로 일치 검사
-   같은 `city + district + locality` 중복 검사
-   깨진 Unicode 폴더명 패턴 검사 — 단, 추출환경 표시가 아니라 원본/최종 ZIP central directory 기준으로 판정
-   ZIP 생성 후 ZIP 엔트리 Unicode 검사
-   부모 구·군 페이지 존재 여부 검사
-   동 페이지 canonical이 실제 계층 URL과 일치하는지 검사
-   sitemap에 신규 동 페이지 포함 여부 검사
-   존재하지 않는 동 페이지 내부링크 검사
-   기존 Hero/본문/SEO/ALT/Schema 검사 유지

동 단위 확장 후에도 `QA PASS` 전에는 업로드 파일을 전달하지 않는다.

------------------------------------------------------------------------

## 26. 현재 확장 기준선

현재 기준 구조는 다음 원칙으로 고정한다.

-   운영 도시·도시 메인·구·군·동·읍·면 수는 `region_map.csv`에서 동적 집계
-   확장 위치: **기존 구·군 폴더 내부에 동·읍·면 페이지 추가**
-   구·군별 별도 서브도메인 생성: 보류
-   1회 신규 생성량: 도시 서브도메인별 QA 묶음 최대 10페이지. 여러 도시를
    동시에 진행할 때도 도시별 묶음을 분리하여 각각 누적 검수
-   모든 신규 작업:
    `region_map.csv + progress.md + 실제 HTML + validate_regions.py`
    기준으로 추적
-   내부링크: `generate_internal_links.py`로 10개 재계산
-   sitemap: `generate_sitemap.py`가 map의 published 행만 사용
-   GitHub Actions: sitemap 임시 생성 후 전체 QA가 통과한 경우에만 반영
-   한글 경로 escape/깨짐: 배포 금지 오류

앞으로 도시 서브도메인이 추가되더라도 동일한 계층 및 관리 원칙을
적용한다.

------------------------------------------------------------------------

## 27. 강화 분량 검수 적용 방식 — 2026-08-14 확정

`validate_regions.py`는 `region_map.csv`의 `created_date`가
`2026-08-14` 이상인 페이지에 강화 분량 기준을 자동 적용한다.

-   Hero 90자 미만 또는 180자 초과: 오류
-   지역 고유본문 450자 미만 또는 650자 초과: 오류
-   Hero + 지역 고유본문 합계 550자 미만: 오류
-   기존 페이지: 종전 320~550자 범위 유지

신규 페이지의 `created_date`를 비우거나 과거 날짜로 기록하여 강화 검사를
우회해서는 안 된다. 신규 묶음은 분량 검사뿐 아니라 기존 전체 페이지와의
누적 유사도, title·description 중복, 내부링크, map, sitemap 검사를 모두
통과한 후에만 배포한다.

------------------------------------------------------------------------

## 28. Sitemap `lastmod` 보존 규칙 — 2026-08-14 확정

하단 내부링크 10개를 재계산했다는 이유만으로 기존 페이지의 sitemap
`lastmod`를 작업일로 일괄 변경하지 않는다. 내부링크 박스는 사이트 탐색을 위한
자동 생성 영역이며, 핵심 본문이 수정된 것으로 간주하지 않는다.

-   신규 페이지: 실제 생성일을 `lastmod`로 기록
-   기존 페이지의 Hero·지역 고유본문·title·description 등 검색 핵심 내용 수정:
    실제 수정일로 갱신
-   기존 페이지의 `.region-link-box`만 자동 재배치: 기존 `lastmod` 유지
-   공통 CSS 등 표현 전용 변경: 기존 `lastmod` 유지
-   ZIP처럼 Git 이력을 읽을 수 없는 작업환경: 기존 sitemap 날짜를 보존하고,
    확인할 수 없는 날짜를 임의로 오늘 날짜로 변경하지 않음

`generate_sitemap.py`는 기존 sitemap의 URL별 날짜를 먼저 읽는다. Git 이력이
있으면 내부링크 박스와 표현 전용 영역을 제외한 실제 내용 차이를 비교하고, Git
이력이 없으면 기존 URL 날짜를 그대로 유지한다. 기존 URL 전체의 날짜가 작업일로
일괄 변경된 sitemap은 검수 실패로 보고 배포하지 않는다.

배포 전에는 이전 sitemap과 새 sitemap을 대조하여 다음을 확인한다.

-   신규 URL만 신규 날짜를 받았는지
-   실제 핵심 내용이 수정된 기존 URL만 날짜가 갱신됐는지
-   내부링크 변경만 있었던 기존 URL 날짜가 보존됐는지
-   삭제 URL 잔존, 신규 URL 누락, 동일 URL 중복이 없는지
